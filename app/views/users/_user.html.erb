<li>
  <%= gravatar_for user, size: Settings.gravatar.users_size %>
  <%= link_to user.name, user %>
  <% if current_user.admin? && !current_user?(user) %>
|   <%= link_to t(".delete"), user, method: :delete,
        data: { confirm: t(".delete_confirmation") } %>
  <% end %>
</li>
